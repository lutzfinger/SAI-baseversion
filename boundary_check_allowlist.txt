# Boundary linter exemption list.
# One path per line, each entry MUST be followed by a comment explaining why
# the file is permitted to contain otherwise-flagged content.

HANDOFF.md
# Phase 0 handoff doc references Lutz_Dev paths in concrete `cd ~/Lutz_Dev/...`
# install instructions for the SAI-baseversion repo on Lutz's Mac. The doc is
# instructional / setup-specific.

PHASE-0-DONE.md
# Phase report describing where artifacts landed in Lutz's working tree.

PHASE-1-DONE.md
# Phase report. References lutzfinger / op:// / keychain:// inside backticks
# as documentation of the rule patterns themselves.

app/shared/runtime_env.py
# Framework code that parses keychain:// references. Must mention the scheme
# name to do its job.

scripts/regression_test_email_classifier.py
# Script comment documents the op:// / keychain:// scheme names to explain
# why the runtime env needs to load before Settings reads it.

docs/slack_bot_install.md
# Slack bot install runbook documents the op:// secret-reference scheme
# so operators know how to wire 1Password tokens into runtime.env.

PHASE-2-DONE.md
# Phase 2 report documents the boundary linter's rules — must show examples
# of what each rule catches.

tests/runtime/test_boundary_check.py
# Test file for the boundary linter. Must contain trigger examples for each
# rule (otherwise it can't test them). Not real personal data.

tests/test_disambiguation.py
# Uses 10-digit Unix-epoch-shaped timestamps as Slack message_ts fixtures
# (1700000001, 1700000002, 1700000003). Phone-number regex matches them.
# All synthetic test data, not operator data.

PHASE-3-PARTIAL-DONE.md
# Phase 3 partial report. References the private repo path Lutz_Dev/SAI
# while explaining what landed.

CUSTOMIZE-ME.md
# Customization punch list documents the op:// (1Password CLI) and
# keychain:// (macOS Keychain) reference syntax so users know how to wire
# their own secrets without committing them. Must show the schemes to
# explain them.

PRINCIPLES.md
# The project's core-principles charter. Documents the op:// /
# keychain:// secret-reference convention and the boundary-linter's
# enforcement targets (/Users/..., personal strings) — so it has to
# mention them by name to explain the rules.

SUPPLIERS.md
# References `op://` and `keychain://` schemes inside the
# 1Password / macOS Keychain table rows as legitimate documentation
# of the secret-resolution patterns themselves (not actual references).

README.md
# Public README. References the canonical public repo URL
# (github.com/lutzfinger/SAI-baseversion) — that's the project's
# public identity, documented in PRINCIPLES.md and the public
# article series. Also references `$SAI_PRIVATE` as a placeholder
# for the operator's private overlay path.

docs/onboarding_wizard_prompt.md
# Onboarding wizard prompt for stranger installs. References
# canonical SAI channel names (#sai-eval) and the canonical public
# repo URL.

docs/cowork_skill_creator_prompt.md
# Skill-creator prompt for Co-Work. References canonical SAI
# channel names (#sai-eval) and example skill paths in the
# operator's overlay (Lutz_Dev/SAI/skills/) as concrete examples
# of the canonical layout.

config/.env.example.1password
# Public template for the 1Password backend choice. Documents the
# `op://<vault>/<item>/<field>` and `keychain://sai/...` reference
# schemes — those literal scheme names are exactly what the file
# is teaching the operator to use, so the secret-scheme-reference
# rule must not block them here.

config/.env.example.keychain
# Public template for the macOS Keychain backend choice. Documents
# the `keychain://sai/<account>` reference scheme. Same rationale
# as the 1password template.
