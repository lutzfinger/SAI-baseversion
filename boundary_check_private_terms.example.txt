# Operator-specific terms for the boundary linter — PRIVATE template.
#
# COPY this file to `boundary_check_private_terms.txt` (which is in
# .gitignore) and fill in YOUR operator-specific terms below. All
# matches report under rule `operator-narrative-leak` and fire even
# in allowlisted files — they're operator data and should never
# appear in PUBLIC docs.
#
# Each line is a regex pattern (case-insensitive). Lines starting with #
# are comments. Empty lines are skipped.
#
# What's ALREADY caught by PUBLIC heuristics (you don't need to add
# these — they're built into scripts/boundary_check.py):
#   - "the operator's wife/husband/partner/daughter/son/..." etc.
#   - "{Name}'s setup/inbox/calendar/laptop/notes/..." etc.
#   - The maintainer's own personal-string set (see PERSONAL_STRINGS_RE
#     in scripts/boundary_check.py for what's hard-coded)
#
# What this file is for: OPERATOR-SPECIFIC names + bucket terms +
# company domains that the public heuristics can't know about.
#
# Examples (these are placeholder names — replace with YOUR terms):
#
# # Private contact names (family / colleagues / friends that get
# # mentioned in design narratives):
# \balice\b
# \bbob\b
# \bcharlie\b
#
# # Private bucket / category names from your Gmail labels that
# # should never appear in PUBLIC docs:
# \bcustomers-vip\b
# \bproject-stealth\b
#
# # Operator-specific domains beyond what `personal-string` already covers:
# \byour-company\.com\b
# \byour-personal\.org\b
#
# # Generic operator identifiers beyond what's already in PERSONAL_STRINGS_RE:
# \byour-handle\b
#
# # Common narrative shapes that always indicate a leak in your
# # specific writing style (these vary per operator):
# # \bin my setup\b
# # \bmy fianc[ée]e?\b
# # \bmy startup called \w+\b
