schema_version: "2"

identity:
  workflow_id: sample-alpha-bad-5
  version: "0.1.0"
  owner: framework-tests
  description: "claude_code_subdir contains path characters — validator must refuse."

profiles:
  claude_code:
    enabled: true
    files:
      - SKILL.md
    deploy_to: [claude_code]
    claude_code_subdir: "../../etc"   # path traversal in subdir name
    eval:
      datasets:
        - { kind: canaries, path: profiles/claude_code/canaries.jsonl, fail_mode: hard_fail, min_count: 1 }
