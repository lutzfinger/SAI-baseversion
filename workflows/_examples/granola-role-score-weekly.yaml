workflow_id: example_granola-role-score-weekly
version: "1"
description: Aggregate weekly Granola coaching scores by role and post them to Slack.
worker: granola_score_summary
connector: runtime_only
connector_config:
  cadence: weekly
policy: granola_score_summary.yaml
tags:
  - granola
  - coaching
  - reporting
  - weekly
tools:
  - tool_id: granola_score_summary_slack_sender
    kind: slack_message_sender
    config:
      channel_name: #general
